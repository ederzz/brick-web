import toolTip from './toolTip'
import tost, {success, error, warning, info} from './tost'
import Modal from './modal'
import Scroll from './scroll'

const tosts = {success, error, warning, info}

export {
  Modal,
  Scroll,
  toolTip,
  tost, tosts, // 顶部提示消息
}